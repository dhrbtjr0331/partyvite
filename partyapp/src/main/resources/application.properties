spring.application.name=partyapp

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/party_db
spring.datasource.username=partyapp
spring.datasource.password=usckisatailgate
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=yourSecretKeyMustBeAtLeast256BitsLongForHS256Algorithm12345
jwt.expiration=86400000

# Logging (optional but helpful)
logging.level.org.springframework.security=DEBUG